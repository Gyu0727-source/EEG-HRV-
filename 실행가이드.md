# 🚀 EEG 실시간 분석 시스템 - 실행 가이드

## ✅ 시스템 준비 상태 확인 완료!

다음 사항이 확인되었습니다:

### 📁 파일 구조 (완료)
- ✓ 모든 Python 모듈 (6개) 생성 완료
- ✓ Windows 실행 파일 (3개) 생성 완료
- ✓ HTML 템플릿 생성 완료
- ✓ 모든 폴더 구조 생성 완료

### 📊 테스트 데이터 (준비 완료)
총 **6개** 파일이 "분석 데이터" 폴더에 준비되어 있습니다:
1. 김선근 44 남.xlsx (2.66 MB)
2. 방지수 37 남.xlsx (3.20 MB)
3. 변미정 48 남.xlsx (3.05 MB)
4. 이동미 44 여.xlsx (3.02 MB)
5. 이찬희 28 남.xlsx (3.16 MB)
6. 한진우 38 남.xlsx (3.10 MB)

---

## 🎯 지금 바로 실행하는 방법

### 단계 1: 폴더 열기
파일 탐색기에서 다음 경로로 이동:
```
C:\Users\bach1\OneDrive\문서\05.claude\01.EEG실시간 분석 데이터
```

### 단계 2: 패키지 설치 (최초 1회만)
**방법 1: 배치 파일 사용 (가장 쉬움)**
1. `install_packages.bat` 파일을 **더블클릭**
2. 검은 화면이 나타나면서 패키지가 설치됨
3. "설치가 완료되었습니다!" 메시지 확인
4. 아무 키나 눌러 창 닫기

**방법 2: CMD 사용**
1. 현재 폴더에서 `Shift + 우클릭` → "여기에 PowerShell 창 열기" 선택
2. 다음 명령어 입력:
```powershell
python -m pip install -r requirements.txt
```

### 단계 3: 분석 실행
**방법 1: 배치 파일 사용 (가장 쉬움)**
1. `run_batch.bat` 파일을 **더블클릭**
2. 6개 파일이 자동으로 처리됨 (약 3-5분 소요)
3. 진행 상황이 화면에 표시됨
4. "배치 처리 완료" 메시지 확인

**방법 2: 대화형 모드**
1. `run_interactive.bat` 파일을 **더블클릭**
2. 메뉴에서 옵션 선택
3. 파일 경로 입력

**방법 3: CMD 사용**
```powershell
# 전체 배치 처리
python main.py --folder "분석 데이터"

# 단일 파일만
python main.py --file "분석 데이터/김선근 44 남.xlsx"
```

### 단계 4: 결과 확인
1. `reports` 폴더 열기
2. HTML 파일들을 웹브라우저(Chrome, Edge 등)로 열기
3. 각 사람별 뇌파 분석 보고서 확인!

---

## 📊 생성되는 결과물

### 각 사람마다 10개 파일 생성

#### 1. 원본 백업
```
raw_data/
  └── 김선근_남_44.xlsx
```

#### 2. 분석 데이터 (연구용)
```
analysis_data/
  ├── 김선근_남_44_analysis.csv      ← Excel로 열 수 있음
  └── 김선근_남_44_metadata.json     ← 메타데이터
```

**CSV 파일 내용 예시:**
| time_sec | delta_raw | delta_smooth | theta_raw | theta_smooth | ... |
|----------|-----------|--------------|-----------|--------------|-----|
| 0 | 38.2 | 38.5 | 22.1 | 21.8 | ... |
| 1 | 37.8 | 38.3 | 23.4 | 22.1 | ... |
| 2 | 39.1 | 38.1 | 21.9 | 22.4 | ... |

#### 3. 그래프 (5개 주파수 대역)
```
graphs/
  ├── 김선근_남_44_delta.png   ← 델타파 (깊은 이완)
  ├── 김선근_남_44_theta.png   ← 세타파 (명상)
  ├── 김선근_남_44_alpha.png   ← 알파파 (편안한 각성)
  ├── 김선근_남_44_beta.png    ← 베타파 (집중)
  └── 김선근_남_44_gamma.png   ← 감마파 (인지 활동)
```

#### 4. 최종 보고서 ⭐
```
reports/
  └── 김선근_남_44_report.html   ← 이것을 브라우저로 열기!
```

---

## 🎨 보고서 예시

HTML 보고서에는 다음 내용이 포함됩니다:

### 1. 개인 정보
- 이름, 성별, 나이
- 측정 날짜 및 시간

### 2. 각 주파수 대역별 (5개)
- **그래프**: 시간에 따른 변화 추세선
- **변화점**: 의미있는 2-3개 변화점 자동 표시
  - ↑ 증가 (초록색)
  - ↓ 감소 (빨간색)
  - → 안정 (파란색)
- **자동 해석**: 변화 패턴에 대한 설명
- **통계**: 평균값, 변동계수

### 3. 예시 해석 문구
> "초반에 알파파가 증가한 후 안정화되었습니다. 이상적인 이완 상태를 잘 유지하고 있습니다."

---

## 💡 활용 방법

### 1. 개인별 뇌파 패턴 확인
- 시간에 따른 뇌파 변화 추세 파악
- 의미있는 변화점 확인
- 각 주파수 대역별 특성 이해

### 2. 연구 데이터 분석
- CSV 파일을 Excel이나 Python으로 열기
- 1초 단위 시계열 데이터 활용
- 통계 분석, 그래프 재가공 등

### 3. 여러 사람 비교
- 같은 조건에서 측정한 사람들 비교
- 나이, 성별에 따른 패턴 차이 분석
- 그룹별 평균 계산

### 4. 보고서 공유
- HTML 파일을 이메일로 전송
- 브라우저만 있으면 어디서나 확인 가능
- 인쇄하여 종이 보고서로 활용

---

## ⚙️ 설정 변경 (고급)

`config.py` 파일을 메모장으로 열어서 수정:

### 윈도우 크기 변경
```python
WINDOW_SIZE = 2.0  # 2초 → 3초로 변경 가능
```

### 변화점 개수 조정
```python
N_CHANGE_POINTS = 3  # 3개 → 5개로 변경 가능
```

### 그래프 크기 조정
```python
GRAPH_WIDTH = 14  # 인치
GRAPH_HEIGHT = 5  # 인치
GRAPH_DPI = 300  # 해상도
```

### 색상 변경
```python
COLORS = {
    'delta': '#000080',  # Navy → 원하는 색상 코드
    'alpha': '#228B22',  # 등등...
}
```

---

## 🐛 문제 해결

### "python을 찾을 수 없습니다"
**해결책:**
1. Python이 설치되어 있는지 확인
2. CMD에서 `python --version` 입력하여 확인
3. 없다면 https://python.org 에서 Python 3.12 다운로드

### "No module named 'pandas'" 오류
**해결책:**
1. `install_packages.bat` 파일 실행
2. 또는 CMD에서: `python -m pip install pandas numpy scipy matplotlib openpyxl`

### 파일명 오류
**해결책:**
- 파일명이 `이름_성별_나이` 형식인지 확인
- 예: `김선근_남_44.xlsx` ✓
- 예: `김선근.xlsx` ✗ (성별, 나이 없음)

### 그래프가 안 보임
**해결책:**
- matplotlib 설치 확인: `python -m pip install matplotlib`
- HTML 보고서는 정상 생성됨

### 속도가 너무 느림
**해결책:**
- 정상입니다. 1개 파일당 30초~1분 소요
- 6개 파일: 약 3-5분 소요

---

## 📞 다음 단계

### 1. 즉시 실행
1. `install_packages.bat` 더블클릭
2. `run_batch.bat` 더블클릭
3. `reports` 폴더에서 HTML 파일 확인

### 2. 새로운 데이터 분석
1. 파일명을 `이름_성별_나이.xlsx` 형식으로 변경
2. "분석 데이터" 폴더에 복사
3. `run_batch.bat` 실행

### 3. 설정 커스터마이징
1. `config.py` 파일 수정
2. 윈도우 크기, 변화점 개수 등 조정
3. 다시 실행하여 결과 비교

---

## 📋 체크리스트

- [ ] Python 설치 확인
- [ ] `install_packages.bat` 실행
- [ ] `run_batch.bat` 실행
- [ ] `reports` 폴더에서 HTML 파일 확인
- [ ] 각 주파수 대역별 그래프 확인
- [ ] CSV 파일을 Excel로 열어보기

---

**준비 완료!** 이제 `install_packages.bat`부터 시작하세요! 🎉

더 자세한 정보는 [README.md](README.md) 또는 [빠른시작.md](빠른시작.md)를 참조하세요.
