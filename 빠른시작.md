# 🚀 빠른 시작 가이드

## 3단계로 시작하기

### 1단계: 패키지 설치 (최초 1회만)

**방법 1: 배치 파일 사용 (권장)**
- `install_packages.bat` 파일을 더블클릭
- 설치가 완료될 때까지 대기

**방법 2: 수동 설치**
```cmd
python -m pip install numpy pandas scipy matplotlib openpyxl
```

### 2단계: 데이터 분석 실행

**방법 1: 전체 배치 처리 (권장)**
- `run_batch.bat` 파일을 더블클릭
- "분석 데이터" 폴더의 모든 파일이 자동 처리됨

**방법 2: 대화형 모드**
- `run_interactive.bat` 파일을 더블클릭
- 메뉴에서 원하는 옵션 선택

**방법 3: 명령줄 사용**
```cmd
# 단일 파일
python main.py --file "분석 데이터/김선근 44 남.xlsx"

# 폴더 전체
python main.py --folder "분석 데이터"
```

### 3단계: 결과 확인

다음 폴더에서 결과를 확인하세요:

- **reports/** - HTML 보고서 (브라우저로 열기)
- **graphs/** - 5개 주파수 대역별 그래프
- **analysis_data/** - CSV 시계열 데이터 (연구용)

## 📊 예상 결과

각 개인마다 다음 파일들이 생성됩니다:

```
reports/
  └── 김선근_남_44_report.html  ← 이 파일을 브라우저로 열어보세요!

graphs/
  ├── 김선근_남_44_delta.png
  ├── 김선근_남_44_theta.png
  ├── 김선근_남_44_alpha.png
  ├── 김선근_남_44_beta.png
  └── 김선근_남_44_gamma.png

analysis_data/
  ├── 김선근_남_44_analysis.csv  ← Excel로 열어 시계열 데이터 확인
  └── 김선근_남_44_metadata.json
```

## ⏱️ 처리 시간

- 단일 파일 (15분 측정): 약 30초 ~ 1분
- 6개 파일 배치: 약 3-5분

## ❓ 문제 발생 시

### "python을 찾을 수 없습니다"
- Python이 설치되어 있는지 확인
- 시스템 PATH에 Python이 등록되어 있는지 확인

### "ModuleNotFoundError"
- `install_packages.bat`을 실행하여 패키지 설치
- 또는 수동으로: `python -m pip install numpy pandas scipy matplotlib openpyxl`

### "파일명 형식이 올바르지 않습니다"
- 파일명이 `이름_성별_나이` 형식인지 확인
- 예: `김선근_남_44.xlsx` 또는 `김선근 44 남.xlsx`

### 그래프가 생성되지 않음
- matplotlib 설치 확인: `python -m pip install matplotlib`
- 한글 폰트 문제일 수 있음 (보고서는 정상 생성됨)

## 💡 팁

### 데이터 파일명 규칙
- ✅ 올바른 예:
  - `김선근_남_44.xlsx`
  - `이영희_여_35.xlsx`
  - `김선근 44 남.xlsx`

- ❌ 잘못된 예:
  - `김선근.xlsx` (성별, 나이 없음)
  - `data_001.xlsx` (정보 없음)

### 분석 데이터 활용
- CSV 파일: Excel이나 Python으로 열어서 시계열 분석 가능
- JSON 파일: 메타데이터 (측정 시간, 변화점 정보 등)

### 보고서 커스터마이징
- HTML 템플릿 수정: `templates/report_template.html`
- 설정 변경: `config.py`

## 📞 다음 단계

1. **결과 확인**: `reports/` 폴더의 HTML 파일을 브라우저로 열기
2. **데이터 분석**: `analysis_data/` 폴더의 CSV 파일을 Excel로 열기
3. **설정 조정**: 필요시 `config.py`에서 파라미터 변경
4. **대량 처리**: 새로운 파일을 "분석 데이터" 폴더에 넣고 `run_batch.bat` 실행

---

**더 자세한 정보**: [README.md](README.md) 참조
